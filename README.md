# Circuit extraction in Graphix

Partial implementation of the algorithm presented in Simmons, 2021 (arXiv:2109.05654).

### Installation

`git clone` the repository and run

```bash
cd graphix-circuit-extraction
pip install -e .
```
